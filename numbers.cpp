#include <iostream>

int main() {
   // loop from 1 to 10 and print each number
  for (int i=1; i<=10; ++i) {
      std::cout << i << "";
  }
  std::cout << std::endl; // print the newline after the numbers
  return 0;
  }